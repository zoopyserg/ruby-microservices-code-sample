#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../config/environment'

puts "Loading #{ENV['RACK_ENV']} environment (Sinatra #{Sinatra::VERSION})"

require 'pry'
Pry.start(__FILE__)
